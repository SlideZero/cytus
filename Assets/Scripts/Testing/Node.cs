using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Node : MonoBehaviour
{/*
    public float nodeNote = 0f;
    public int numberOfNodes = 0;
    public GameObject node;
    public GameObject parentNode;
    public float nodeSpacing = 0f;
    public float noteNumber = 0f;
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < numberOfNodes; i++)
        {
            GameObject newNode = Instantiate(node, new Vector2(nodeSpacing, 0), Quaternion.identity);
            newNode.GetComponent<TapNode>().nodeNote = noteNumber;
            nodeSpacing += 5;
            noteNumber += 2;
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
